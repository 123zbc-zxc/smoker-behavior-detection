# 论文最终定稿清单

## 一、正文内容统一
- 核对题目是否统一为“基于深度学习的吸烟者行为检测系统设计与实现”。
- 核对摘要、正文、结论中的模型名称是否统一为 `YOLOv8n` 和 `YOLOv8n+ECA`。
- 核对论文口径是否统一：`baseline` 作为主结果，`ECA` 作为对比实验。
- 核对 PyQt5 的表述是否统一为“本地联调原型”，FastAPI 是否统一为“答辩展示方案”。

## 二、实验数据与结果检查
- 核对 baseline 指标是否统一：P `0.526`、R `0.625`、mAP@0.5 `0.520`、mAP@0.5:0.95 `0.323`。
- 核对 ECA 指标是否统一：P `0.513`、R `0.572`、mAP@0.5 `0.494`、mAP@0.5:0.95 `0.299`。
- 核对分类别结果是否前后一致，特别是 `cigarette`、`smoking_person`、`smoke` 三类。
- 检查第四章中的文字分析是否与表格数据一致，避免“文字说提升、表格却下降”。

## 三、图表与编号
- 检查目录是否生成完整，章节标题和目录标题是否一致。
- 检查所有图片是否居中，图题是否统一为“图X-X 名称”格式。
- 检查所有表格是否有表题，表题格式是否统一为“表X-X 名称”。
- 检查图表在正文中是否都被引用过，避免“有图无解释”。
- 优先保留这些图：YOLOv8 各版本对比图、YOLOv8n 结构图、ECA 模块图、YOLOv8n+ECA 结构图。

## 四、格式与版式
- 检查封面、原创性声明、指导声明、目录页是否齐全。
- 检查各章标题层级是否统一，避免一级标题、二级标题样式混乱。
- 检查段落首行缩进、行距、页边距是否一致。
- 检查中英文标点是否混用过多，英文缩写前后空格是否统一。
- 检查是否还存在乱码、问号标题或异常符号。

## 五、参考文献与附录
- 补齐正式参考文献，不要只保留示例条目。
- 核对参考文献编号与正文引用是否对应。
- 检查参考文献格式是否符合学校要求。
- 附录中保留关键命令即可，不要堆太多无关日志。

## 六、答辩前最后检查
- 通读一遍摘要、第 4 章、第 5 章、第 6 章，确保口吻自然。
- 准备 1 张 baseline 与 ECA 对比表，答辩时直接展示。
- 准备 2 到 4 张检测效果图，最好包含正确检测和误检样例。
- 确认答辩演示口径：先讲数据集，再讲 baseline，最后讲 ECA 和网页展示。
- 最终导出一份可提交版 DOCX，并另留一份备份版本。
